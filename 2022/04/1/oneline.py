with open("input.txt") as f: print(sum((one := set(range(int(pair[0].split("-")[0]), int(pair[0].split("-")[1]) + 1))) <= (two :=  set(range(int(pair[1].split("-")[0]), int(pair[1].split("-")[1]) + 1))) or two <= one for pair in [pair.split(",") for pair in f.read().splitlines()]))
